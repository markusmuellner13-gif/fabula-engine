# Optional. When set, the AI Assistant uses Claude to interpret your game
# descriptions. Without it, the app falls back to a built-in local parser and
# still works fully offline.
ANTHROPIC_API_KEY=
